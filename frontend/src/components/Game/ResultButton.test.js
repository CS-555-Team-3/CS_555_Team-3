import { render, screen } from '@testing-library/react';
import React from 'react';
import ResultButton from './ResultButton'

describe('Does it add to answers', () => {
    test('answers', () => {
        render(<App />);
        ResultButton()
        expect(document.getElementById("button1")).toBe();
    });
});
describe('Does it add to answers', () => {
    test('answers', () => {
        render(<App />);
        ResultButton()
        expect(document.getElementById("button2")).toBe();
    });
});
describe('Does it add to answers', () => {
    test('answers', () => {
        render(<App />);
        ResultButton()
        expect(document.getElementById("button3")).toBe();
    });
});

describe('Does it add to answers', () => {
    test('answers', () => {
        render(<App />);
        ResultButton()
        expect(document.getElementById("button4")).toBe();
    });
}); describe('Does it add to answers', () => {
    test('answers', () => {
        render(<App />);
        ResultButton()
        expect(document.getElementById("button5")).toBe();
    });
});